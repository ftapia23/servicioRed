Comandos basicos para usuar los repos de GIT

Crear una carpeta para tu nuevo repo
	mkdir path/n_dir
Iniciar git
	git init
Colocarte en la posición del nuevo directorio, posteriormente
realizar un checkout del proyecto que necesitamos (remoto)
	git clone username@host:/path/repo
	Ejemplo
	git clone https://github.com/ftapia23/gestion-empresarial.git

Si no quieres clonarlo, y sólo quieres conectarlo a un repo remoto utiliza
el siguiente comando (Sugerencia, utilizar el paso anterior)
	git remote add origin <server>

Adjuntar archivo
	git add archivo

Hacer commit
	git commit -m "Comentario, para dar a conocer los cambios"

Actualizar el repo, al último commit de los colaboradores
	git pull

Subir tus cambios
	git push origin master	
